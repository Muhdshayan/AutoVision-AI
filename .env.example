# Place this file at the repository root (next to frontend/ and backend/).
# backend/main.py and Vite (frontend) both load from here.

# Python API (backend/main.py) — preferred name
GEMINI_API_KEY=

# If GEMINI_API_KEY is empty, the backend also reads:
# VITE_GEMINI_API_KEY=

# Optional (backend reads GEMINI_MODEL or VITE_GEMINI_MODEL)
# GEMINI_MODEL=gemini-2.5-flash
# GEMINI_FALLBACK_MODELS=gemini-2.5-flash-lite
