# Python API (server/main.py) — preferred name
GEMINI_API_KEY=

# If GEMINI_API_KEY is empty, the server also reads this (same file as before)
# VITE_GEMINI_API_KEY=

# Optional (server reads GEMINI_MODEL or VITE_GEMINI_MODEL)
# GEMINI_MODEL=gemini-2.5-flash
# GEMINI_FALLBACK_MODELS=gemini-2.5-flash-lite
